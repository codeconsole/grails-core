{
  "properties": [
    {
      "name": "grails.banner.art.display",
        "description": "Whether to display the Grails banner art.",
      "type": "java.lang.Boolean",
      "defaultValue": true
    },
    {
        "name": "grails.banner.art.file",
        "description": "The file path on the classpath to the Grails banner art to display.",
        "type": "java.lang.String",
        "defaultValue": "grails-banner.txt"
    },
    {
      "name": "grails.banner.versions.display",
      "description": "Whether to display version information in the Grails banner.",
      "type": "java.lang.Boolean",
      "defaultValue": true
    },
    {
      "name": "grails.banner.versions.include",
      "description": "A list of optional versions to include in the banner.",
      "type": "java.util.List<grails.boot.GrailsBanner.OptionalVersionOption>"
    },
    {
      "name": "grails.banner.versions.exclude",
      "description": "A list of versions to exclude from the banner.",
      "type": "java.util.List<grails.boot.GrailsBanner.DefaultVersionOption>"
    },
    {
      "name": "grails.banner.versions.order",
      "description": "A sorted list of versions to put first in the banner versions.",
      "type": "java.util.List<grails.boot.GrailsBanner.VersionOption>"
    },
    {
      "name": "grails.web.removeDefaultViewResolverBean",
      "description": "Whether to remove Spring Boot's defaultViewResolver bean so Grails' own view resolution is used.",
      "type": "java.lang.Boolean",
      "defaultValue": true
    },
    {
      "name": "grails.web.removeWelcomePageMapping",
      "description": "Whether to remove Spring Boot's welcome-page handler mapping so Grails' URL mappings own the root path ('/') rather than serving a static index.html for it.",
      "type": "java.lang.Boolean",
      "defaultValue": true
    },
    {
      "name": "grails.i18n.localeResolver",
      "description": "The locale resolution strategy. 'session' and 'cookie' are mutable (the ?lang= switch works); 'acceptHeader' and 'fixed' are read-only (the ?lang= switch is ignored).",
      "type": "java.lang.String",
      "defaultValue": "session"
    },
    {
      "name": "grails.i18n.include-plugin-bundles",
      "description": "Whether message bundles contributed by plugins participate, both in message resolution and in the locales offered by a language selector. Bundle caching, encoding and locale fallback are configured through spring.messages.* instead.",
      "type": "java.lang.Boolean",
      "defaultValue": true
    },
    {
      "name": "grails.i18n.default.locale",
      "description": "The locale of the base message bundle, always offered by a language selector and used by the 'fixed' locale resolver. Falls back to the JVM default when unset.",
      "type": "java.lang.String"
    },
    {
      "name": "grails.i18n.cache.seconds",
      "description": "How long (in seconds) resolved message bundles are cached. Honoured only when reload is enabled, as before.",
      "type": "java.lang.Integer",
      "deprecation": {
        "level": "warning",
        "reason": "Spring Boot owns the message source; its cache duration is configured with spring.messages.cache-duration.",
        "replacement": "spring.messages.cache-duration"
      }
    }
  ],
  "hints": [
    {
      "name": "grails.i18n.localeResolver",
      "values": [
        { "value": "session", "description": "Resolve the locale from the HTTP session (mutable; ?lang= switching works). The default." },
        { "value": "cookie", "description": "Resolve the locale from a cookie (mutable; ?lang= switching works)." },
        { "value": "acceptHeader", "description": "Resolve the locale from the Accept-Language header (read-only; ?lang= is ignored)." },
        { "value": "fixed", "description": "Use a fixed locale from grails.i18n.default.locale, falling back to the JVM default (read-only; ?lang= is ignored)." }
      ]
    }
  ]
}