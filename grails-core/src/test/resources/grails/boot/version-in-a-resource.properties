# Stands in for a library that records its version in a resource it ships, the way Tomcat records
# its own in org/apache/catalina/util/ServerInfo.properties. Kept here rather than read from a real
# dependency so that the test does not change meaning when a managed version is bumped.
server.number=1.2.3
server.built=not a version
