package com.example;

// A stand-in for a Grails ApplicationCommand. It carries no framework dependency so the cli
// source set compiles offline; the command contract is irrelevant to the discovery being tested.
public class HelloWorldCommand {
}
