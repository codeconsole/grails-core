# Licensed to the Apache Software Foundation (ASF) under one or more
# contributor license agreements.  See the NOTICE file distributed with
# this work for additional information regarding copyright ownership.
# The ASF licenses this file to You under the Apache License, Version 2.0
# (the "License"); you may not use this file except in compliance with
# the License.  You may obtain a copy of the License at
#
#     https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

projectVersion=8.0.0-SNAPSHOT

javaVersion=21

githubBranch=8.0.x
githubSlug=apache/grails-core

# Note: BOM dependencies are defined in dependencies.gradle files to allow for dependabot updates

# Project dependency versions not managed by BOMs
apacheDsVersion=1.5.4
apacheMavenVersion=3.9.16
apacheMavenResolverVersion=1.9.27
flapdoodleVersion=4.33.0
mongoJavaServerVersion=1.47.0
commonsValidatorVersion=1.10.1
concurrentlinkedhashmapLruVersion=1.4.2
defaultElImplementationVersion=5.0.0
# See https://projects.eclipse.org/releases/jakarta-10 and latest 5.0.x from https://mvnrepository.com/artifact/jakarta.el/jakarta.el-api
elApiVersion=5.0.1
expectitCoreVersion=0.9.0
geantyrefVersion=1.3.16
gparsVersion=1.2.1
# Keep gradle version synced with .sdkmanrc, all gradle-wrapper.properties files,
# and grails-forge/grails-forge-core/src/main/java/org/grails/forge/feature/build/gradle/templates/gradleWrapperProperties.rocker.raw
gradleToolingApiVersion=9.7.1
greenmailVersion=2.1.14
javassistVersion=3.33.0-GA
jnrPosixVersion=3.2.4
joddWotVersion=3.3.8
joptSimpleVersion=5.0.4
jspApiVersion=4.0.0
openTest4jVersion=1.3.0
picocliVersion=4.7.7
slf4jVersion=2.0.17
spyMemcachedVersion=2.12.3

# Build dependency versions not managed by BOMs
apacheRatVersion=0.8.1
gradleChecksumPluginVersion=1.4.0
gradleCycloneDxPluginVersion=3.0.0

# Libraries only specific to test apps, these should not be exposed
ersatzVersion=4.0.1
jbossTransactionApiVersion=2.0.0.Final

# build dependencies for code quality checks
checkstyleVersion=13.11.0
codenarcVersion=4.0.0
jacocoVersion=0.8.15
pmdVersion=7.27.0
spotbugsPluginVersion=6.4.8
sonatypeScanPluginVersion=3.1.6

# This prevents the Grails Gradle Plugin from unnecessarily excluding slf4j-simple in the generated POMs
# https://github.com/apache/grails-gradle-plugin/issues/222
slf4jPreventExclusion=true

org.gradle.caching=true
# Configuration cache is explicitly disabled until known issues are resolved.
# See: https://github.com/apache/grails-core/issues/15497
org.gradle.configuration-cache=false
org.gradle.parallel=true
org.gradle.daemon=true
# Do NOT turn on due to https://github.com/gradle/gradle/issues/9489
#org.gradle.configureondemand=true
# This -Xmx sizes the Gradle DAEMON only. Test forks are separate child JVMs and get their
# own heap from maxHeapSize in gradle/test-config.gradle (768m on CI, 1024m locally), so a
# rough lower bound on a job's configured heap is:
#
#     daemon -Xmx  +  (concurrent test forks x per-fork maxHeapSize)
#
# "Concurrent test forks" is NOT maxParallelForks. With org.gradle.parallel=true several
# Test tasks run at once, so the live fork count is bounded by Gradle's global worker pool
# (--max-workers, defaulting to the CPU count).
#
# This is a simplified CONFIGURED-HEAP budget, not a true peak: it excludes metaspace and
# other native memory, the Gradle client, and forked Java/Groovy compiler workers (which
# CompilePlugin gives their own -Xmx2G). Treat it as a floor when sizing a runner.
#
# Documentation does NOT run on this heap. Every groovydoc task is launched as a JVM of its
# own by GroovydocEnhancerPlugin, and the user guide in a worker process by PublishGuideTask.
# That is what lets this number be 3G: before, groovydoc kept a Groovy runtime per documented
# module alive inside the daemon for the whole build, which needed 5G and still exhausted it
# on the smallest runner.
org.gradle.jvmargs=-Dfile.encoding=UTF-8 -Xmx3G

# Those doc JVMs size themselves: 1g per module by default, raised by grails-doc and
# grails-data-docs/stage for their aggregates, and by grails-doc for the guide. To get a
# documentation run that will not fit moving without editing a build script, pass
# -PgroovydocMaxHeapSize=<size> or -PguideMaxHeapSize=<size>; either beats the build script.
